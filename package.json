{
  "name": "tslua2",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tstl -p tsconfig.json",
    "build:proto": "cd protocols && npm run build",
    "build:tables": "cd tables && npm run build",
    "test": "jest",
    "tslua": "npx tsx cli/cli.ts"
  },
  "devDependencies": {
    "typescript-to-lua": "^1.34.0",
    "typescript": "^5.0.0",
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
