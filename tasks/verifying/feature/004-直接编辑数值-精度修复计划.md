# #004 精度修复计划 v3（完整审查后）

## 问题本质

浮点数在 **UI ↔ 内存 ↔ 配置文件** 往返时精度丢失。
用户输入 0.91 → 保存到 cfg → 重启加载 → 变成 0.919（默认值）。

**根因**：序列化链路中 `slider.Value (double) → (float) cast → BarData.LengthScale (float) → (double) cast → ConfigFile`，float 作为中间环节导致精度丢失。

## 修复方案：定点整数序列化

**只改序列化层**，不改默认值、不改内存中的 float 表示。

- 写 cfg 时：`float值 × 10000 → int`，存整数
- 读 cfg 时：`int / 10000.0f → float`，还原
- 内存中仍然是 float，UI/逻辑代码完全不变

### 精度因子

- **SCALE = 10000**（万分之一精度，比 Step=0.001 多一位）
- 0.91 → 9100 → 0.91f ✅
- 0.919 → 9190 → 0.919f ✅

## 全局配置读写审计

**共享同一个 cfg 文件**：`user://debug_panel_config.cfg`

| # | 读写者 | 读写内容 | Section 格式 | 需要修复？ |
|---|--------|----------|-------------|-----------|
| 1 | DebugPanel | 版本号 + 面板几何 | meta, panel_geo | 需更新版本号 |
| 2 | EntityProfileManager | Profile 数据 | profile_X.* | **主要修复** |
| 3 | DebugPanelMapTab | 地图/摄像机参数 | map, camera | **不需要**（double→double→double） |
| 4 | DebugPanelUITab | UI 参数 | skill_bar, fn_bar, buff_bar | **不需要**（全是整数） |
| 5 | MonsterManager | 怪物样式 | monster_* | **次要修复**（legacy 路径） |
| 6 | NpcManager | NPC 样式 | npc_* | **不需要**（全是整数偏移） |
| 7 | Player.cs | Player 样式 | player | **次要修复**（legacy 路径） |
| 8 | BarControlGroup | 血条控件 | ? | **不需要**（死代码，无引用） |
| 9 | ScreenshotTool | 复制文件 | 全部 | **不需要**（只复制不解析） |

## v2→v3 自审修正（新增 3 个问题）

| # | v2 问题 | v3 修正 |
|---|---------|---------|
| 8 | **版本号冲突**：DebugPanel.CONFIG_VERSION=2，EntityProfileManager.ConfigVersion=3，共享同一文件。DebugPanel 每次保存写 version=2，EntityProfileManager 读到 2<3 触发迁移（但迁移是空操作）。**v4 迁移永远不会触发**，因为 DebugPanel 总是把版本号写回 2 | 统一版本号：DebugPanel.CONFIG_VERSION 和 EntityProfileManager.ConfigVersion 都改为 4。DebugPanel.SaveConfig 写 version=4，EntityProfileManager.LoadConfig 读到 4=4 不触发迁移。旧文件（v2/v3）读到 version<4 时触发迁移 |
| 9 | **BarControlGroup 是死代码**：没有任何活跃代码引用它，Phase 3 不需要 | 删除 Phase 3（BarControlGroup），改为备注说明它是死代码 |
| 10 | **DebugPanelMapTab 不需要修复**：它的 slider.Value 是 double，cfg 存 double，读回 double，没有 float 中间环节，无精度问题 | 明确标注 MapTab 不需要修复，避免误改 |
| 11 | **MonsterManager 和 Player.cs 的 legacy 路径也需要更新**：虽然 monster_* 和 player section 在新格式下不再写入，但旧文件可能还有这些 section，MonsterManager._Ready 和 Player.cs 在 EntityProfileManager 之前读取它们 | MonsterManager.LoadStyleConfigFromSection 和 Player.cs 的 config.GetValue 也需要用 ReadFp 兼容读取，否则旧文件迁移后第一次启动时读 int 值会崩 |

## 修改清单

### Phase 1：基础设施 + 版本号统一

**`EntityProfileManager.Serialization.cs`** — 新增定点转换辅助方法

```csharp
private const int FpScale = 10000;
private static int ToFp(float v) => Mathf.RoundToInt(v * FpScale);
private static int ToFpD(double v) => (int)Math.Round(v * FpScale);
private static float FromFp(int v) => v / (float)FpScale;

/// 安全读取：兼容旧格式(double)和新格式(int)
private static int ReadFp(ConfigFile config, string section, string key, int defaultFp)
{
    var v = config.GetValue(section, key, defaultFp);
    if (v.VariantType == Variant.Type.Int) return (int)v;
    if (v.VariantType == Variant.Type.Float) return ToFpD((double)v);
    return defaultFp;
}
```

**`EntityProfileManager.cs`** — ConfigVersion 3 → 4

**`DebugPanel.cs`** — CONFIG_VERSION 2 → 4（统一版本号）

### Phase 2：序列化改为定点整数（主要修复）

**`EntityProfileManager.Serialization.cs`** — WriteComponentData / ReadComponentData

写（所有比例类字段）：
```csharp
// 旧：config.SetValue(section, "length_scale", (double)bar.LengthScale);
// 新：config.SetValue(section, "length_scale", ToFp(bar.LengthScale));
```

读（所有比例类字段）：
```csharp
// 旧：LengthScale = (float)(double)config.GetValue(section, "length_scale", 102.0 / 111.0),
// 新：LengthScale = FromFp(ReadFp(config, section, "length_scale", ToFp(102.0f / 111.0f))),
```

**受影响的字段（仅比例类，0~2 范围，Step=0.001）：**

| Section 类型 | Key | 旧默认值(double) | 新默认值(定点int) |
|-------------|-----|-------------------|-------------------|
| appearance | visual_size_scale | 1.0 | 10000 |
| appearance | border_width_scale | 3.0/111.0 ≈ 0.02703 | 270 |
| appearance | bg_opacity | 0.9 | 9000 |
| healthbar/mpbar | length_scale | 102.0/111.0 ≈ 0.91892 | 9189 |
| healthbar/mpbar | height_scale | 6.0/111.0 ≈ 0.05405 | 541 |
| healthbar/mpbar | fill_percent | 1.0 | 10000 |
| castbar | length_scale | 60.0/111.0 ≈ 0.54054 | 5405 |
| castbar | height_scale | 4.0/111.0 ≈ 0.03604 | 360 |
| castbar | fill_percent | 0.0 | 0 |

**不受影响的字段（整数步长，无精度问题）：**
- visible, center_x, force_show (bool)
- offset_x, offset_y (整数步长 1)
- corner_radius (整数步长 1)
- font_size (整数)
- color_r/g/b (取色器交互)
- label 相关字段
- monster_ai, npc_interact 字段

### Phase 3：Legacy 路径兼容（次要修复）

**`MonsterManager.cs` — LoadStyleConfigFromSection**

比例类字段也用 ReadFp 兼容读取：
```csharp
// 旧：cfg.HpBarLengthScale = (float)(double)config.GetValue(section, "hp_bar_length_scale", cfg.HpBarLengthScale);
// 新：cfg.HpBarLengthScale = FromFp(ReadFp(config, section, "hp_bar_length_scale", ToFp(cfg.HpBarLengthScale)));
```

MonsterManager 的比例类字段：
- visual_size_scale, border_width_scale, bg_opacity
- hp_bar_length_scale, hp_bar_height_scale, hp_bar_fill_percent
- mp_bar_length_scale, mp_bar_height_scale, mp_bar_fill_percent

**注意**：MonsterManager 没有 WriteStyleConfig（只读不写），所以不需要改写入侧。但需要兼容读取 int 格式（迁移后的文件）。

**`Player.cs`** — legacy config 读取

```csharp
// 旧：float scale = (float)(double)config.GetValue("player", "visual_size_scale", 1.0);
// 新：float scale = FromFp(ReadFp(config, "player", "visual_size_scale", 10000));
```

比例类字段：visual_size_scale, border_width_scale

**`EntityProfileManager.Legacy.cs`** — legacy 格式加载

旧格式读取后值是 double，走 ReadFp 的 double 分支自动兼容，无需额外代码。
但需要把 `(float)(double)config.GetValue(...)` 改为 `FromFp(ReadFp(config, ...))`。

### Phase 4：配置版本迁移

**`EntityProfileManager.Config.cs`** — MigrateConfig 补充 v3→v4 迁移逻辑

```csharp
private void MigrateConfig(ConfigFile config, int fromVersion)
{
    // v0-v2 → v3: 旧格式自动转换在新格式加载逻辑中处理
    
    // v3 → v4: 比例类字段从 double 转为定点整数
    if (fromVersion < 4)
    {
        var scaleKeys = new HashSet<string> {
            "visual_size_scale", "border_width_scale", "bg_opacity",
            "length_scale", "height_scale", "fill_percent",
            "hp_bar_length_scale", "hp_bar_height_scale", "hp_bar_fill_percent",
            "mp_bar_length_scale", "mp_bar_height_scale", "mp_bar_fill_percent",
        };
        foreach (string section in config.GetSections())
        {
            foreach (string key in config.GetSectionKeys(section))
            {
                if (!scaleKeys.Contains(key)) continue;
                var v = config.GetValue(section, key, 0);
                if (v.VariantType == Variant.Type.Float)
                    config.SetValue(section, key, ToFpD((double)v));
            }
        }
        GD.Print("[EntityProfileManager] Migrated scale values to fixed-point format (v3→v4)");
    }
}
```

**`DebugPanel.Config.cs`** — MigrateConfig 也需要补充 v3→v4

DebugPanel 的 MigrateConfig 目前只处理 v0→v1 和 v1→v2。需要补充 v3→v4 的定点整数迁移（与 EntityProfileManager 的逻辑相同）。

**时序保证**：
- EntityProfileManager._Ready 在 DebugPanel 之前执行
- EntityProfileManager.LoadConfig 先执行迁移，再读取数据
- DebugPanel.LoadConfig 也会执行迁移（双重保险）
- 迁移后 config.Save 由 EntityProfileManager 完成（但实际不 Save，只读取）
- 下次 DebugPanel.SaveConfig 时写入 version=4 + 定点整数格式

### Phase 5：SyncToData 精度对齐

**`BarGroupComponent.cs`** — SyncToData 中对齐到定点网格

```csharp
// 旧：LengthScale = (float)_lengthScaleSlider.Value
// 新：LengthScale = FromFp(ToFpD(_lengthScaleSlider.Value))
```

**`AppearanceComponent.cs`** — SyncToData 同理

```csharp
// 旧：VisualSizeScale = (float)_sizeScaleSlider.Value
// 新：VisualSizeScale = FromFp(ToFpD(_sizeScaleSlider.Value))
```

比例类字段全部对齐：VisualSizeScale, BorderWidthScale, BgOpacity, LengthScale, HeightScale, FillPercent

整数类字段不改：CornerRadius, FontSize, OffsetX, OffsetY

**`CastBarComponent.cs`** — SyncToData 同理

### Phase 6：验证

1. 编译 0 错误
2. 启动游戏，打开调试面板
3. 修改血条长度比例为 0.91，确认 cfg 文件中存储为 `length_scale=9100`
4. 重启游戏，确认加载后仍为 0.91
5. 拖动滑条到任意位置，确认保存/加载一致
6. 旧配置文件（v2/v3，double 格式）自动迁移到 v4（int 格式）
7. Monster 样式配置读取兼容（int 和 double 都能读）
8. 版本号统一：DebugPanel 和 EntityProfileManager 都写 version=4

## 不改的部分

- **默认值**（`102.0f/111.0f` 等）— 保持原样
- **ComponentData 类** — 字段类型仍是 float
- **EntityBase.cs** — 属性默认值不变
- **EntityProfile.cs** — CreateDefault 不变
- **EntityStyleConfig.cs** — 默认值不变
- **DebugPanelMapTab** — double→double→double，无精度问题
- **DebugPanelUITab** — 全是整数，无精度问题
- **NpcManager** — 整数偏移，无精度问题
- **BarControlGroup** — 死代码，无引用
- **Color 字段** — 取色器交互，不走滑条输入

## 风险评估

- **低风险**：只改序列化层，内存中的 float 表示和 UI 逻辑完全不变
- **向后兼容**：ReadFp 兼容旧 double 格式 + 新 int 格式
- **版本迁移**：v2/v3 → v4 自动升级，DebugPanel 和 EntityProfileManager 版本号统一
- **可逆**：如果出问题，只需把序列化改回 double 格式，cfg 文件会自动重写
- **唯一注意**：ReadFp 必须用 Variant.VariantType 判断类型，不能直接 (int) 强转 double Variant