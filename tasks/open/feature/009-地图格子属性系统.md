# 009-地图格子属性系统（Phase 1：配置表 + 客户端渲染）

- **类型**: feature
- **状态**: open
- **优先级**: P1
- **模块**: 客户端渲染 + Luban 配置表
- **创建**: 2026-05-16
- **分支**: `task/009-地图格子属性系统`

## 陛下决策确认

1. ✅ 先搞 Phase 1（配置表 + 客户端渲染）
2. ✅ 减速幅度支持配置就好（策划可调）
3. ✅ 水域暂时不可走
4. ✅ 视觉后续提供，先显示地形名称文字标签
5. ✅ 地图编辑器里编辑地形，导出到表格，走已有机制更新

## Phase 1 任务清单

- [x] Luban 新增 `TerrainConfig` bean（地形属性定义）
- [x] 新增 `TbTerrainConfig.xlsx`（9种地形数据）
- [x] `build_tables.ts` 生成地形配置 JSON
- [x] 客户端 `GridCell` 绑定 Luban 配置（`RefreshFromConfig`）
- [x] `GridManager` 渲染改配置驱动（颜色从 Luban 读取）
- [x] 地形名称文字标签显示（编辑模式/鼠标悬停）
- [x] 地图编辑器地形刷选工具（已有 PaintTerrain，自动刷新配置）
- [x] 编译通过 + 测试通过

## Phase 2 预留（待后续开单）

- [ ] 服务端 `MapState.Tiles` 数据结构
- [ ] 服务端移动系统接入地形检查（`IsTileWalkable`）
- [ ] 地形减速效果
- [ ] 地形同步协议 `MapInfoSyncNotify.tiles`
- [ ] 战斗系统地形影响（元素加成/防御修正）
- [ ] HP/MP 自然恢复（泉水地形）

## 已知问题

- 当前 `GridCell` 有 `TerrainType` 字段，但 `GridManager.DrawWalkableOverlay()` 颜色是硬编码
- 需要把硬编码颜色改为从 Luban `TerrainConfig` 读取
- 水域（TerrainType=1）当前在 `IsWalkable` 检查中没有特殊处理，需确保不可走

## 验证

- [x] 编译 0 错误 0 警告（build_tables.ts + Godot C#）
- [ ] 启动后地图正确渲染地形颜色
- [ ] 编辑模式可切换地形笔刷
- [ ] 鼠标悬停/放大显示地形名称
- [ ] 保存/加载地图后地形数据不丢失

## 关联文档

- `docs/design/地图格子属性系统设计.md`
- `docs/tech/Luban配置表管理规范.md`
