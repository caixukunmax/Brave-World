# Brave-World 待验证功能清单（2026-05-14）

> 生成时间：2026-05-14 14:03
> 项目状态：代码全部就绪，等待陛下启动 Godot + Gateway 进行实际运行验证
> 当前分支：`task/004-精度修复`，提交 `8702948`

---

## 📋 验证前准备

1. **启动服务端**：运行 `C:\code\tslua2\servercsharp\restart.bat`（或 `.estart.ps1`）
2. **启动 Godot 编辑器**：打开 `C:\code\tslua2\clinetcsharp\project.godot`
3. **启动 Gateway**：从 VS Code 运行 Gateway 项目，或运行 `.un-gateway.ps1`
4. **编译确认**：服务端已编译通过（0 警告 0 错误），客户端无需额外编译

---

## 🔴 P0 修复验证（5 项，无需陛下确认，我已可修复，先验证再修）

### P0-1 HP 同步链路断裂
**验证步骤**：
1. 登录游戏，进入地图，记住当前 HP
2. 让怪物攻击，使 HP 下降（不要满血）
3. 等待 5 分钟（AutoSave 周期）或手动触发保存（如切换地图）
4. 退出游戏重新登录
5. **期望**：HP 保持下降后的值（非满血）
6. **现状（bug）**：退出再登录后 HP 恢复满血

**修复状态**：未修复，代码已就绪（2 行改动）

---

### P0-2 NpcManager 信号泄漏
**验证步骤**：
1. 进入游戏，找到 NPC（转职大师）对话一次
2. 切换场景（如回登录再进游戏，或换地图）
3. 再次与 NPC 对话
4. **期望**：对话窗口正常弹出一次
5. **现状（bug）**：可能弹出多个对话窗口（信号重复绑定）

**修复状态**：未修复，代码已就绪（3 个信号 -=）

---

### P0-3 DebugPanelSystemTab 信号泄漏
**验证步骤**：
1. 按 F12 打开 DebugPanel
2. 切换到 SystemTab，点击几次按钮
3. 关闭 DebugPanel（再按 F12）
4. 重新打开 DebugPanel，切换到 SystemTab
5. 点击按钮
6. **期望**：按钮只触发一次动作
7. **现状（bug）**：可能触发多次（信号重复绑定）

**修复状态**：未修复，代码已就绪（9 个信号 -=）

---

### P0-4 DebugPanelEntityTab 信号泄漏
**验证步骤**：
1. 按 F12 打开 DebugPanel
2. 切换到 EntityTab，选择一个实体，编辑数值几次
3. 关闭 DebugPanel
4. 重新打开，切换到 EntityTab
5. 编辑数值
6. **期望**：每次编辑只触发一次保存
7. **现状（bug）**：可能触发多次保存

**修复状态**：未修复，代码已就绪（8 个信号 -=）

---

### P0-5 NetworkManager 缓存不同步
**验证步骤**：
1. 登录游戏，打开背包，记住物品数量
2. 使用一个物品（如药水）或丢弃一个物品
3. 物品栏应减少一个
4. 切换地图（触发 MapInfoSync）
5. **期望**：背包显示使用/丢弃后的数量
6. **现状（bug）**：切换地图后物品数量恢复（缓存未更新）

**修复状态**：未修复，代码已就绪（4 个 Handler 加缓存更新）

---

## 🟢 已完成任务验证（8 项，需陛下确认后移到 done/）

### V-1 #004 精度修复
**验证步骤**：
1. 按 F12 打开 DebugPanel
2. 切换到 EntityTab，选择一个实体
3. 修改某个 float 值（如 Scale、X、Y）为精确小数（如 0.91、1.2345）
4. 点击保存
5. 关闭 DebugPanel
6. 重新打开 DebugPanel，选择同一实体
7. **期望**：数值保持精确（0.91 不是 0.919，1.2345 不是 1.234）
8. **验证通过标准**：修改、保存、关闭、重开，数值不变

**状态**：代码完整，编译通过，待验证

---

### V-2 #006 怪物样式保存
**验证步骤**：
1. 按 F12 打开 DebugPanel
2. 切换到 EntityTab，选择一个怪物
3. 修改怪物样式参数（Scale、Color、BorderWidth 等）
4. 点击保存
5. 关闭 DebugPanel，重新打开
6. **期望**：怪物样式保持修改后的值
7. 切换地图后回来，检查怪物样式是否保持

**状态**：代码完整，编译通过，待验证

---

### V-3 Buff 系统 Phase 1（基础框架）
**验证步骤**：
1. 进入战斗，使用带 Buff 的技能（如战士"战吼"、法师"毒雾"、牧师"神圣护盾"）
2. 观察角色或怪物是否获得 Buff 图标/状态
3. **期望**：Buff 正确施加，持续时间内有效

**状态**：代码完整，编译通过，待验证

---

### V-4 Buff 系统 Phase 2（DOT + 属性修正）
**验证步骤**：
1. 使用中毒类技能（如"毒雾"或"猛毒撕咬"）
2. 观察目标是否每回合掉血（DOT 效果）
3. 使用属性加成技能（如"战吼"），观察攻击力是否提升
4. **期望**：DOT 每 tick 减血，属性加成正确生效

**状态**：代码完整，编译通过，待验证

---

### V-5 Buff 栏 UI
**验证步骤**：
1. 获得任意 Buff（如战吼、护盾、中毒）
2. 观察屏幕上的 Buff 栏是否显示 Buff 图标
3. Buff 消失后，图标是否移除
4. **期望**：Buff 栏正确显示/移除图标

**状态**：代码完整，编译通过，待验证

---

### V-6 掉落拾取系统 Phase 2（联调验证）
**验证步骤**：
1. 击杀一个怪物
2. **期望**：地面上出现掉落物（物品图标）
3. 走到掉落物附近
4. **期望**：自动拾取，物品进入背包
5. 打开背包确认物品已添加

**状态**：代码完整，编译通过，待验证（需服务端 DropManager 注册 + 死亡回调绑定）

---

### V-7 #005 MonsterTab 组件化
**验证步骤**：
1. 按 F12 打开 DebugPanel
2. 切换到 MonsterTab
3. 检查是否能正常显示怪物列表
4. 选择一个怪物，修改参数
5. **期望**：MonsterTab 功能正常，无异常

**状态**：代码完整，编译通过，待验证

---

### V-8 buff-independent-notify
**验证步骤**：
1. 进入战斗，施加/受到 Buff
2. 观察 Buff 通知是否独立于其他系统正确推送
3. **期望**：Buff 状态变化时，客户端正确收到通知

**状态**：代码完整，编译通过，待验证

---

### V-9 gm-panel-item-display
**验证步骤**：
1. 按 F12 打开 DebugPanel
2. 切换到 GM Tab
3. 检查物品显示是否优化（如品质颜色、图标、数量）
4. **期望**：GM 面板物品列表显示美观、信息完整

**状态**：代码完整，编译通过，待验证

---

## 🔵 开放任务验证（2 项，需陛下确认后推进）

### O-1 Buff 栏 UI 完整功能
**前置条件**：V-3、V-4、V-5 验证通过
**验证步骤**：
1. Buff 栏正确显示多个 Buff
2. Buff 持续时间倒计时显示
3. 鼠标悬停显示 Buff 详细信息
4. Buff 到期后自动移除
5. **期望**：Buff 栏 UI 完整可用

**状态**：Phase 1/2/3 代码完整，待联调验证

---

### O-2 掉落拾取系统 Phase 3（端到端验证）
**前置条件**：V-6 验证通过
**验证步骤**：
1. 击杀多种怪物（不同等级/类型）
2. 观察掉落物种类和数量是否符合配置表
3. 拾取后检查背包物品是否正确
4. **期望**：掉落系统完整可用

**状态**：服务端代码就绪，需客户端联调

---

## ⚠️ 已知 bug 验证（1 项，陛下确认后修复）

### B-1 SetPreferredSkill msgId+1 bug
**验证步骤**：
1. 打开技能面板，设置优先技能
2. 观察是否成功保存
3. 退出游戏重新登录
4. **期望**：优先技能保持设置
5. **现状（bug）**：优先技能功能完全不可用（协议不兼容）

**修复方案**：A. 改 proto 让 RSP=400（需双端同步）；B. GatewayService 改显式映射
**状态**：待陛下确认修复方案

---

## 🛡️ 安全修复验证（可选，建议上线前）

### S-1 TokenSecret 硬编码
**修复后验证**：检查仓库中 appsettings.json 不包含密钥，改用环境变量

### S-2 登录自动注册
**修复后验证**：配置文件中可关闭自动注册

### S-3 MD5 签名改 HMAC-SHA256
**修复后验证**：Token 生成/验证使用新算法，旧 Token 失效

---

## 📝 验证完成后的行动清单

| 验证项 | 通过后操作 |
|--------|-----------|
| V-1 #004 精度修复 | 合并 `task/004-精度修复` 分支到 master |
| V-2 #006 怪物样式保存 | 任务移到 `done/bug/` |
| V-3~V-9 | 任务移到 `done/feature/` |
| P0-1~P0-5 | 我立即执行修复（无需额外确认） |
| B-1 msgId+1 | 按选定方案修复（需确认方案 A 或 B） |
| S-1~S-3 | 按 `docs/tech/安全修复方案.md` 执行 |

---

## 📊 项目状态总览

| 类别 | 数量 | 状态 |
|------|------|------|
| 待验证已完成任务 | 8 | 代码就绪，等陛下验证 |
| 待验证开放任务 | 2 | 代码就绪，等陛下验证 |
| 待验证已知 bug | 1 | 等陛下确认修复方案 |
| P0 待修复（我可直接修） | 5 | 等陛下验证环境就绪 |
| P1 待修复（需陛下确认） | 8 | 等陛下确认 |
| P2 待修复 | 17 | 等陛下确认 |
| P3 设计/架构 | 13 | 等陛下决策 |

**总计：38 个问题待处理，10 个任务待验证**

**阻塞原因：100% 依赖陛下启动 Godot + Gateway 进行实际运行验证**

---

*本清单由狗剩 14:03 唤醒检查时生成，用于陛下上线后快速验收*
