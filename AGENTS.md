# Agent 工作指南

## 文档驱动工作流

1. **需求先行**
   - 在动手改代码之前，先确认当前需求是否有对应的需求/设计文档。
   - 如果有文档，先修改文档，确保新需求与旧需求没有冲突。
   - 如果发现新需求与已有设计冲突，或需求本身不合理，必须与用户确认后再继续。
   - 如果没有文档，先落实文档（Markdown 格式，通常放在 `docs/design/` 下），用户确认后再进入代码实现。

2. **代码实现**
   - 只有在需求文档（或设计文档）明确、无冲突、且已得到确认后，才开始写代码。
   - 代码改动必须与文档保持一致。如果实现过程中发现文档需要调整，先更新文档再改代码。

---

## 版本控制铁律

1. **未经用户明确指令，禁止擅自提交或推送代码。**
   - 包括但不限于：`git commit`、`git push`、`git merge`、`git rebase`、`git reset`、`git revert` 等会修改仓库历史或共享状态的操作。
   - 即使需求已经实现、测试已经通过，也必须在得到用户的明确提交/推送指令后才能执行。
   - 用户此前的任何一次批准（如“先把目前的 push 一下”）仅对那一次操作有效，不代表后续改动可以自动提交。

2. **提交前必须先展示变更摘要。**
   - 执行 `git status`、`git diff --stat` 或等效命令，让用户看到将要提交的内容。
   - 区分功能代码、生成文件、配置文件和无关变更（如构建工具产生的临时文件）。

3. **禁止 force-push 等破坏性操作，除非用户明确授权。**
   - 误提交到远程后，默认使用 `git revert` 等安全方式回滚，不 rewrite 共享历史。

---

## Bug 修复与防御性复盘机制

**触发条件**：用户提出修复 Bug、处理报错、代码表现不符合预期时，必须在给出修改方案前运行此框架。

### 修复前灵魂三问

**1. 溯源：这是新问题，还是历史债的爆发？**
- 拒绝"头痛医头"：不要只看报错的那一行代码。异常抛出点往往不是真正的根因。
- 追问历史：这段代码之前能跑，是因为逻辑严谨，还是仅仅因为运气好/调用路径没覆盖到？
- 识别隐性错误：之前的代码是否在设计之初就存在隐患（如跨服务时序问题、边界条件未考虑），只是被特定的业务数据掩盖了，直到今天才触发？
- **本项目特有风险**：Godot 客户端预测与 C# 服务端权威的冲突、Protobuf 序列化/反序列化字段遗漏。

> **⚠️ 关键：在溯源代码之前，先退一步审视需求本身。**
> - 需求描述是否清晰、完整、无歧义？
> - 需求内部是否存在逻辑矛盾（例如：要求"怪物既能巡逻又能原地不动"、要求"服务启动时就预热尚未启动的服务"）？
> - 这个"Bug"是否实际上是需求本身的 Bug，而不是代码的 Bug？
> 
> **如果怀疑需求有问题，立即停止代码修改，先与用户沟通、理顺需求后再继续。** 强行按有问题的需求去改代码，只会制造更多 Bug。

**2. 治本：如何彻底消灭，而不是贴创可贴？**
- 警惕防御性补丁：禁止只加一个 `if` 判断或 `try-catch` 吞掉异常来"消灭报错"。这只会让 Bug 变成更难追踪的幽灵。
- 结构级修复：修复是否需要调整数据结构、重构函数职责、或者修正状态机的流转？
- 副作用评估：这个"根本性修复"会不会导致依赖它的其他模块崩溃？（如是，说明系统耦合度过高，需要一并解耦）。
- **本项目特有风险**：修改 `GameConstants` 基础值会影响所有依赖它的逻辑；修改 proto 定义需同步客户端和服务器。

**3. 免疫：如何让系统"获得抗体"，永不复犯？**

针对不同问题的通用性，采取不同层级的防御手段：

**层级一：代码级免疫**
- 在关键路径上加**防御性断言**，让隐性错误显性化。
- 提取**通用工具函数**统一处理（如参数校验、错误码统一返回）。

**层级二：流程级免疫**
- 补充**单元测试**（特别是触发此次 Bug 的边界条件）。
- 在 `AGENTS.md` 中增加**约束规则**（如"禁止在 init 中预热未启动的服务"）。

**本项目特有问题清单（规则已浓缩，详细背景见对应 Memory）**
- 服务器代码改动须过 `restart.bat` 编译运行验证；数值优先放 Luban 表而非硬编码。
- 怪物 AI 新增能力须先分 `Overworld/Combat` 高层模式，禁止把战斗分支堆进 `patrol/patrol_chase` 非战斗 AI。
- 改游戏核心状态（`WorldState`、移动预占、`CombatManager`/`MonsterManager` 集合、在线状态）必须经 `IGameLoopScheduler` 入队串行执行，禁止在多线程 handler/tick/callback 直接改。
- Godot `CanvasItem` shader 的 `fragment()` **禁止 `return`**，统一 `if-else` 链；无地形格子背景勿设透明，免网格线看不清。
- 服务器高频 tick（`MonsterTick`/`CombatTick`）热路径禁 `LogInformation`，定位用 `LogDebug`。
- 所有 `MapService.PlayerEnter` 入口须完整填 `PlayerSnapshot`（`EquippedSkills`/`Job`/`MoveSpeedMs`）；新增 `MapPlayerState` 字段同步更新所有调用点。
- 被编辑器插件/预览复用的运行时 Node 脚本须加 `[Tool]`；运行时副作用用 `Engine.IsEditorHint()` 守卫。
- 编辑器内嵌 `SubViewport` 画布禁 `Stretch=true+StretchShrink=1` 跟容器；监听 `Resized` 手动同步 `Size`，初始 `Size` 给小值并重写 `_GetMinimumSize()`。
- 编辑器 Dock 主面板若继承普通 `Control`，根容器须 `SetAnchorsPreset(FullRect)`；固定宽高比视口外包 `AspectRatioContainer`。
- 底部 Dock 内容超高会向下溢出盖住标签栏；主面板须 FullRect + `ClipContents=true` + 内容列外包 `ScrollContainer`。
- 实体 `EnsureRenderComponents` **禁止 `_renderComponents.Count>0` 早退**，须按类型幂等补齐（`GetRenderComponent<T>()==null` 才加）。
- 实体预览必须走 `EntityPreviewFactory.CreatePreviewEntity`，类型判定走 `EntityPreviewPolicy.ResolveKind`；禁各面板 new 预览实体或重复 switch。
- 编辑器 `SubViewport` 预览须对齐宿主渲染环境（像素密度/主题），统一走 `EditorPreviewEnvironment`。
- 滑条数值显示须持有 `SliderValueInput.Attach` 返回的 `Button`，禁保留被替换的旧 `Label`（否则值脱节、点一下跳变）。

**层级三：认知级免疫（Memory / Skill 沉淀）——【重点执行项】**
- **判断是否需要写入 Memory（记忆）**：如果这是一个认知盲区、架构原则或业务陷阱，立即将其写入记忆文件。
- **判断是否需要提炼为 Skill（技能）**：如果这是一个可重复执行的标准动作流，将其提炼为 Skill 指令集，在未来类似任务中自动调用。

### 输出格式要求

在给出具体代码修改方案时，按以下结构输出，让用户看到思考深度：

**🐛 【表象与根因】**
- 报错原因：...
- 根本原因：...（说明是否是历史遗留问题暴露）

**🛠️ 【根治方案】**
- 为什么不采用 XX 简单修复（说明创可贴方案的危害）
- 采用 XX 方案的原因及代码修改：...

**🛡️ 【系统免疫】**
- 通用性分析：这是一个局部 Bug 还是一个通用模式？
- 防御措施：补充了 XX 日志 / 增强了 XX 断言 / 更新了 AGENTS.md 规则。

**🧠 【记忆更新】**
- 如果触发认知级免疫，明确告知用户："我发现这是一个通用陷阱，已将『XXX 规则』写入 `AGENTS.md`，防止以后再犯。"
